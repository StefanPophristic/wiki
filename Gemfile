source 'https://rubygems.org'

gem "jekyll", "~> 4.3.3"
gem "jekyll-remote-theme", "~> 0.4.3"

gem "jekyll-seo-tag"  # Add this line

gem "jekyll-include-cache"  # Add this line

gem 'csv' #chatgpt said to put this after bundle exec jekyll build returned an error about needing csv

gem 'base64' #also chatgpt request after it threw an error
 
